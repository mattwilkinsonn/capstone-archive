mutation Register($input: Register!) {
  register(input: $input) {
    user {
      id
      username
      email
      createdAt
      updatedAt
    }
    error {
      field
      message
    }
  }
}
